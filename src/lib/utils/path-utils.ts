import { AUTH_ROUTES, EXCLUDE_PATHS } from '@/lib/constants';

// Auth routes that don't require authentication
const authRoutes = AUTH_ROUTES;
const excludePaths = EXCLUDE_PATHS;

export { authRoutes, excludePaths };
